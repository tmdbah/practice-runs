<!-- When updating this file, follow the format below and don't remove the comments -->

# Current Feature: Onboarding Tour

## Merge Target

main

## Status

<!-- Not Started|In Progress|Completed -->

Completed

## Goals

<!-- Goals & requirements -->

Phase 4's first slice: a short, dismissible, first-visit onboarding walkthrough so a brand-new player learns the core loop without asking in the group chat. Full implementation plan: `/Users/tmdbah/.claude/plans/recursive-riding-river.md`.

- Resolves the open question in `practiceRuns-ProjectOverview.md` (coach-marks library vs. scripted first-session state) in favor of scripted first-session state: the tour spotlights the player's own real Monday cell and has them actually tap/save it (their row starts all-`UNAVAILABLE`, so this is also their first real data entry), rather than narrating over a passive overlay. No new dependency (no react-joyride/driver.js/shepherd.js).
- Shown once, only immediately after a brand-new player picks their name for the first time (`NamePicker`'s `onSelect`) — never for returning players with a valid stored identity. Persisted via a `hasSeenTour` flag in `localStorage`, key `practice-runs:tour:${slug}`, mirroring the existing per-team identity mechanism (`use-identity.ts`) exactly, including its dual-`useSyncExternalStore` hydration-safe pattern.
- **v2 resequence (current):** five steps, causally ordered per user feedback on v1's flow — `usual` (spotlight the player's own Monday cell, grid forced into Usual mode, real tap→save) → `toggle` (spotlight the toggle bar, real tap advances, forced into this-week mode regardless of which button) → `overrides` (spotlight the *same* Monday cell, now shown faded/inherited — explain-only, no second edit, "Next") → `team-window` (spotlight the Team Window card, copy references the actual gold-highlight/swipe UI, "Next") → `sessions` (spotlight the Game Day + Sessions wrapper, owned by `TeamGrid` not `AvailabilityGrid` since that section lives outside the grid component, "Got it"). A "Step X of 5" indicator was added to the tooltip. "Skip tour" available at every step; `hasSeenTour` is written only on skip or the final "Got it", never mid-flight.
- New files only: `src/lib/tour-steps.ts` (pure step-sequencing reducer + copy), `src/lib/tour-position.ts` (pure tooltip-placement math), `src/hooks/use-tour.ts` (localStorage persistence), `src/components/OnboardingTour.tsx` (presentational spotlight+tooltip overlay). `TeamGrid.tsx` and `AvailabilityGrid.tsx` get additive changes (new optional props, refs on already-existing elements, a second `OnboardingTour` mount in `TeamGrid` for the `sessions` step); `NamePicker.tsx`, `GridCell.tsx`, `TeamWindowCard.tsx`, `EditDrawer.tsx`, `SessionsView.tsx`, `SessionsSection.tsx` are unmodified.
- Zero backend/schema impact — no Prisma migration, no API route changes.

## Notes

<!-- Any extra notes -->

Guide-to-tap (spotlight + instruct) was chosen over auto-opening `EditDrawer` programmatically: the tap itself is the muscle-memory a player needs every week, and `EditDrawer` is already a fully controlled/presentational component, so guide-to-tap needs zero changes to it — the tour just listens for the real `onSave`/toggle-click to fire its own advance events.

Visual design reuses only existing `@theme` tokens (`bg-surface`, `border-border-strong`, `ring-accent`, etc.); the one exception is `getBoundingClientRect()`-derived `top/left/width/height` via inline `style`, justified the same way `EditDrawer`'s existing raw `bg-black/60` backdrop already deviates from the token system for the same reason (computed pixel values can't be static Tailwind classes).

Accepted, documented limitation: browser back/forward mid-tour does not resume an interrupted tour (`tourStep` is in-memory only, not persisted) — a returning-with-valid-identity remount never re-triggers `NamePicker`, so there is no resume hook. Not worth persisting partial step state for a one-time, low-stakes flow.

Touches `src/components/TeamGrid.tsx` and `src/components/AvailabilityGrid.tsx` (existing files) plus 4 new files — went through a `/plan` pass per `CLAUDE.md`'s >3-files rule (plan approved, see the plan file linked above).

The `sessions` step's target (Game Day + Sessions) lives outside `AvailabilityGrid.tsx`, so `TeamGrid.tsx` owns a second, independent `OnboardingTour` mount + its own tiny ref/measurement effect for that one step, rather than relocating all tour ownership up to `TeamGrid`. The one cross-component concern this introduces — a curious tap on an unrelated grid cell during the `sessions` step opening a real `EditDrawer` alongside `TeamGrid`'s own tour tooltip — is handled by a new `onDrawerOpenChange` callback prop on `AvailabilityGrid` (fired from its existing `openDrawer`/`closeDrawer`), letting `TeamGrid` hide its tour while the drawer is open, mirroring `AvailabilityGrid`'s own `!activeEdit` guard for its four steps.

## History

<!-- Keep this updated. Earliest to latest -->

- 2026-07-19: Feature documented, scope clarified with user (Phase 1 schema only, no NextAuth, no Venue/Session/Rsvp), status set to In Progress
- 2026-07-19: Implemented on `feature/prisma-neon-setup`. Installed Prisma 7.8.0 + `@prisma/adapter-pg` + `pg`; ran `prisma init` with v7's `prisma.config.ts` format. Wrote `prisma/schema.prisma` (Team/Player/DayDefault/DateOverride, Status enum, cascade deletes, indexes). Since v7 removed `datasource.directUrl`, `prisma.config.ts` uses `DIRECT_URL` (Neon's unpooled connection, needed by the schema engine) while the runtime client in `src/lib/prisma.ts` uses `DATABASE_URL` (Neon's pooled connection) via `@prisma/adapter-pg`. Ran `prisma migrate dev --name init` against the user's Neon database successfully. Verified the pooled connection end-to-end with a temporary API route (create+delete round trip), then removed it. `npm run lint` and `npm run build` both pass. Status set to Completed — ready for commit per ai-interaction.md workflow.
- 2026-07-19: Phase 1 — Core Availability Grid completed on `phase-1-core-availability-grid`. Built seed script (demo team, 8 fake players), GET /api/teams/[slug] read API, localStorage identity with useSyncExternalStore, 7-col availability grid UI, bottom-sheet tap-to-edit drawer with optimistic saves, and PATCH /api/teams/[slug]/players/[playerId]/default write endpoint. 23 unit tests covering schedule-filling logic, PATCH validation, and identity hook. Merged to main.
- 2026-07-19: Phase 2 — This Week overrides + team window completed on `phase-2-this-week-overrides-team-window`. Extended GET API with thisWeek DayCell[] (effectiveStatus = DateOverride ?? DayDefault, isOverridden flag) and teamWindows[] per day; added PATCH /override endpoint; added Usual/This Week toggle with inherited (faded) vs overridden (dot) cell styling; added TeamWindowRow component with live optimistic window recalculation. 22 new unit tests. Merged to main.
- 2026-07-19: Post-Phase-2 fixes completed on `fix/this-week-sync-and-default`. Fixed dark-mode time-picker icon visibility, Usual→This Week optimistic sync (inherited cells and their team windows now merge live `usualOverrides` instead of the stale server snapshot), changed default grid mode to This Week, and fixed uneven column widths via `table-fixed`. `npm run lint`, `npm run build`, and all 45 existing unit tests pass. Merged to main.
- 2026-07-19: Team Window Carousel completed on `feature/team-window-carousel` (spec: `context/features/completed/feature-team-window-carousel-spec.md`). Replaced the This Week grid's per-day table row (`TeamWindowRow`, deleted) with a standalone `TeamWindowCard`: defaults to the best-availability day (tie-break: earliest date; all-zero falls back to day 0), navigable via always-visible arrow buttons (disabled at the ends, no wraparound), native touch swipe (CSS scroll-snap), and 7 tappable dot indicators. Underlying `computeWindowForDate` calculation untouched — display-layer only. Also updated `practiceRuns-ProjectOverview.md`, `practiceRuns-ProjectPlan.html`, `AGENTS.md`, and `feature-phase-2-spec.md` (superseded note) to keep docs in lockstep with the new design, and corrected stale "Phase 2 not started" status text left over from prior merges. 4 new unit tests for the default-day selection logic; `npm run lint`, `npm run build`, and all 49 unit tests pass. Merged to main.
- 2026-07-19: TeamWindowCard positioning/style fix on `fix/team-window-card-position`, based on a mockup screenshot comparison. Moved the card above the grid table (was rendering below it) and restyled from a tall stacked card with large flanking arrows into a compact single-line box (label + day/time pill + "{count} free · best overlap" caption), matching the mockup's "Team window · Fri 7–9pm" look — carousel functionality (arrows, swipe, dots) unchanged. `bestIndex` now computed via a lazy `useState` initializer instead of a ref, since the `react-hooks/refs` lint rule disallows reading a ref during render. `npm run lint`, `npm run build`, and all 49 unit tests pass. Merged to main.
- 2026-07-19: TeamWindowCard layout/mount-sync fix on `fix/team-window-card-layout-and-mount-sync`, from a second screenshot review. Fixed content bleed (next slide's text peeking in on the right edge): arrow buttons were `absolute`-positioned overlays compensated for with `px-7` padding on the scroll container, while slides were `w-full` against that same padded box — the unpadded visible viewport was wider than a slide, exposing a sliver of the next one. Replaced with true flex-sibling arrows (`shrink-0` buttons + `flex-1 min-w-0` scroll area, no padding trick). Also fixed the carousel visually opening on day 0 regardless of the computed best-availability day — the default index only set React state, nothing actually scrolled the container there, so the dot indicator looked desynced from the visible card. Added a `useLayoutEffect` that jumps to the default index with `behavior: "instant"` before first paint. `npm run lint`, `npm run build`, and all 49 unit tests pass. Merged to main.
- 2026-07-19: Reset-to-Usual fix, committed directly to `main` (skipped the branch/PR workflow this once). Bug: once a player overrode a This Week day, there was no way back to "inherit Usual" short of manually re-entering Usual's exact values. Added `DELETE /api/teams/[slug]/players/[playerId]/override?date=YYYY-MM-DD` (validates date format, `deleteMany` is idempotent) and a "Reset to Usual" button in `EditDrawer` shown only when the open cell is already overridden in This Week mode; `AvailabilityGrid.handleReset` does the optimistic revert-to-inherited + rollback-on-failure. Also redesigned `NamePicker` from a tap-list to a `<select>` + Continue button (matches the doc's original mockup) and centered `TeamGrid` with a `max-w-md` wrapper instead of full-bleed. New tests for the DELETE route; docs (`practiceRuns-ProjectOverview.md` Routes/API + Decisions log, `practiceRuns-Architecture.md` §5.2) updated retroactively on 2026-07-20 to reflect this — see below.
- 2026-07-20: Phase 3 — Sessions & Venues completed on `phase-3-sessions-venues`. Added Venue/Session/Rsvp models + VenueType enum with two migrations; seeded INSZN as first venue. Built admin venue form (/admin/venues/new), full sessions CRUD API (GET, POST, PATCH, DELETE, RSVP upsert), and SessionsView client component with propose/edit form, optimistic RSVP, live cost-split + headcount display for RENTED_GYM, and proposer-only edit/delete with inline confirmation. Also: NamePicker replaced native select with tappable list; TeamGrid gained stale-identity auto-clear + Switch button; shared sessionInclude/toSessionResponse helper extracted to src/lib/sessions.ts. 28 new tests (97 total). Merged to main.
- 2026-07-20: Accessibility + demo-roster fix, also committed directly to `main`. Bug: the availability grid's `<table>`/`table-fixed` markup couldn't give per-breakpoint column sizing once the demo roster was realistically sized, and lacked explicit grid semantics. Re-implemented `AvailabilityGrid` as CSS Grid with explicit `role="table"/"row"/"columnheader"/"rowheader"/"cell"` attributes (no visual regression, same cell components). `TeamWindowCard` now highlights (gold background/text) when the visible day is the best-overlap day and auto-follows the carousel to the new best day when an edit changes which day that is (previously only positioned correctly on mount). Expanded `prisma/seed.ts` demo roster from 8 to 15 players to match the real team's size. Added Neon MCP tool-call rules to `AGENTS.md`. Docs (`practiceRuns-ProjectOverview.md`, `practiceRuns-Architecture.md`, `practiceRuns-ProjectPlan.html`) updated retroactively on 2026-07-20 for both this and the prior entry, per user request to reconcile main with the docs after skipping the doc-first workflow for two commits.
- 2026-07-20: Bug report — "Unexpected end of JSON input" when creating a session, on `phase-3-sessions-venues`. Root cause was two-fold: (1) the dev server had been running since before the Sessions/Venues API routes existed on this branch, so `POST .../sessions` 500'd with an empty body — fixed by restarting `next dev` (no code change). (2) A real, separately-discovered defect in `src/hooks/use-identity.ts`: `isLoaded` was computed as `typeof window !== "undefined"`, which is `true` on the client's very first (hydration-matching) render — exactly the anti-pattern React's own hydration-mismatch message names — so every page load rendered `TeamGrid`'s loading placeholder on the server but jumped straight to `NamePicker`/the grid on the client, throwing a hydration mismatch on every load. Fixed by giving `isLoaded` the same `useSyncExternalStore` dual-snapshot treatment already used for `playerId` in the same hook (server snapshot `false`, client snapshot `true`) — a plain `useState`+`useEffect` "mounted flag" was tried first but rejected by the project's `react-hooks/set-state-in-effect` lint rule. Verified via `npx vitest run` (6/6 hook tests), a clean Playwright pass (0 console errors on load and after proposing a session), and a full propose → RSVP → delete round trip through the real UI. `npm run lint` and the full 54-test suite pass.
- 2026-07-20: Edit session (Goal 6) implemented on `phase-3-sessions-venues`, planned via `/plan` per CLAUDE.md's `src/app/` rule. Extracted the `SessionResponse` mapping logic (previously duplicated 3×across GET/POST/rsvp-PUT) into `src/lib/sessions.ts` (`sessionInclude` + `toSessionResponse`, typed via `Prisma.SessionGetPayload`), then added a `PATCH` handler on `sessions/[sessionId]/route.ts` reusing it — whole-record replace semantics (mirrors `CreateSessionBody`), no server-side proposer check (matches the existing DELETE/RSVP trust model), and RSVPs are structurally untouched since `data` never references the `rsvps` relation. Added `EditSessionBody` to `types/api.ts`. `SessionsView.tsx`'s propose form now doubles as the edit form via a new `editingId` state var (`startEdit(session)` pre-fills all fields including `date.slice(0, 10)` for the date input; `handleFormSubmit` branches POST-vs-PATCH; `resetForm()` clears `editingId` on cancel/success so it can't leak into the next "+ Propose"); a proposer-only "Edit" button sits next to "Delete" in the session card. New test file `sessions/[sessionId]/route.test.ts` (10 cases, including an explicit regression test that pre-seeded RSVPs survive a PATCH untouched). `npm run lint`, `npx vitest run` (64/64), and `npm run build` all pass; verified end-to-end via Playwright (propose → RSVP in → Edit → change time/cost → Save → RSVP and new values both correct → Delete to clean up). User committed manually (`6785e42`).
- 2026-07-20: Backfilled `DELETE` test coverage on `sessions/[sessionId]/route.ts` — the original PATCH-adding pass had explicitly scoped this out as pre-existing/untouched code, but the user asked to close that gap. Added 5 cases to the existing `route.test.ts` (404 team not found, 404 session not found, correct team-scoped `findFirst` lookup, 204 + correct `prisma.session.delete` call on success, empty response body) following the same mock/`makeParams` pattern as the PATCH tests. `npm run lint`, `npx vitest run` (69/69), and `npm run build` all pass.
- 2026-07-20: Session grid indicators implemented on `feature/session-grid-indicators`, planned via `/plan` per CLAUDE.md's `src/app/` rule (touched `TeamGrid.tsx`/`SessionsView.tsx`/`AvailabilityGrid.tsx`, more than 3 files). Lifted `sessions` state from `SessionsView` up into `TeamGrid` (single owner, passed to both `AvailabilityGrid` and `SessionsView` via props) so grid indicators update live after propose/RSVP/delete — `page.tsx` untouched, `SessionsView`'s internal mutation logic (`handleDelete`/`handleFormSubmit`/`handleRsvp`) untouched beyond the prop rename. Added three exported pure helpers to `AvailabilityGrid.tsx` (`groupSessionsByDate`, `hasSessionOnDate`, `isPlayerRsvpdIn`, keyed on `session.date.slice(0,10)` matching the grid's existing UTC-midnight date convention), reusing the existing `getTeamWindow(dayOfWeek)` helper for column-to-date lookup. Column headers get a gold pill accent (reusing the player-number-badge styling) when a session exists that date; player cells get a gold checkmark badge (`bottom-0.5 right-0.5`, opposite the existing top-right override/note dot) when that player RSVP'd `ANYTIME` for a session that date — both apply identically in Usual and This Week modes. Checkmark renders as a sibling `<span>` in the existing `role="cell"` wrapper (matching the save-error-indicator precedent), not as a new `GridCell` prop, keeping `GridCell.tsx` availability-only. 11 new unit tests in `AvailabilityGrid.test.ts` (118 total, all passing); `npm run lint` and `npm run build` also pass. Verified end-to-end via Playwright against the real `demo-team` seed data (pre-existing INSZN session + Amir's RSVP): header accent and checkmark render correctly against real DB data, both markers coexist without overlap when a This-Week override is added to the same cell, checkmark disappears live on RSVP-out while the header accent correctly persists (session still exists, just 0 "in"), indicators persist identically across Usual/This-Week toggle — all confirmed via DOM/className inspection since screenshot capture was timing out in this environment. Test edits (override + RSVP-out) were reverted via the UI's own "Reset to Usual"/RSVP-In actions and confirmed clean via direct SQL against the dev branch afterward. User committed manually and merged to `main` (`314c420`).
- 2026-07-20: Session status (Proposed/Confirmed/Cancelled) feature documented, planned via `/plan` per CLAUDE.md's schema-file rule. Prompted by a real incident: a proposed `RENTED_GYM` session never hit `minPlayers` and the venue slot got taken by another team before anyone knew to react. Plan approved — see Goals above and `/Users/tmdbah/.claude/plans/staged-squishing-toucan.md`. Status set to In Progress.
- 2026-07-20: Session status implemented on `feature/session-status`. Added `SessionStatus` enum (`PROPOSED`/`CONFIRMED`/`CANCELLED`, default `PROPOSED`) + `Session.status` field via migration `20260720225120_add_session_status`; added `status` to `SessionResponse`/`toSessionResponse`. Two new dedicated no-body `PATCH` endpoints — `.../confirm` and `.../cancel` — reuse `sessionInclude`/`toSessionResponse`, are idempotent on repeat calls, and (confirm only) reject transitioning out of `CANCELLED`; mirrors the existing `DELETE .../override` precedent rather than overloading the whole-record-replace edit `PATCH`. `AvailabilityGrid.tsx`'s `hasSessionOnDate`/`isPlayerRsvpdIn` now exclude `CANCELLED` sessions, and a new `isSessionConfirmedOnDate` helper drives a three-state header pill: solid gold (confirmed), hollow gold outline (proposed), muted (none/cancelled-only). `SessionsView.tsx` gained `handleConfirm`/`handleCancel` (not optimistic, matching Edit/Delete's precedent — a mistaken "booked" flash would mislead the team), a `startAlternate()` that pre-fills the propose form from a cancelled session's venue/cost/minPlayers with date/time left blank (creates a new session via POST, doesn't touch the cancelled one), proposer-only "Mark as Booked"/"Cancel" buttons (Cancel gets the same inline confirm-swap UX as Delete), a "Booked ✓" badge, and a greyed-out "This slot fell through" card state that hides RSVP/Edit but keeps the RSVP list as a historical record and the proposer's Delete button. Renamed the pre-existing Delete-confirm's abort button from "Cancel" to "Never mind" to avoid reading as the same action as the new session-status Cancel. 25 new unit tests (12 `AvailabilityGrid.test.ts` cases, 13 across the two new route test files); `npm run lint`, `npx vitest run` (140/140), and `npm run build` all pass. Verified end-to-end via Playwright against `demo-team`'s seeded INSZN session: confirmed a proposed session (badge + solid pill appeared), cancelled it (banner + greyed card + pill disappeared, RSVP history preserved), used "Propose alternate time here" (form pre-filled venue/cost/minPlayers with blank date/time, new session landed as `PROPOSED` with its own hollow pill) — hit one unrelated snag along the way, a stale pre-existing dev server (PID from before this session, running a Prisma client generated before the schema change) 500'd on the first confirm attempt; killed both stray dev servers and restarted clean, same root cause as a prior bug in this project's history. Cleaned up test data (deleted the alternate session via the UI, restored the original session's status to `PROPOSED` via direct SQL against the dev branch since there's no un-cancel UI action by design) and confirmed the demo team matches its pre-test baseline. Docs (`practiceRuns-ProjectOverview.md` Data/Routes/Decisions, `practiceRuns-Architecture.md` §4.1/§5.3/§7) updated to match. Status set to Completed — ready for commit per ai-interaction.md workflow.
- 2026-07-20: Quick fix, same branch — the propose/edit session form's native date/time picker icons rendered black-on-black against the dark theme (only `EditDrawer.tsx`'s time inputs had previously been fixed for this, per the Post-Phase-2 dark-mode fix; `SessionsView.tsx`'s form was missed). Added the same `[color-scheme:dark]` Tailwind arbitrary property already established as this project's fix for the issue to all three inputs (date, from-time, to-time). Verified via computed style (`color-scheme: dark` on all three) since screenshot capture times out in this environment, same limitation noted in an earlier history entry. `npm run lint`, `npx vitest run` (140/140), and `npm run build` all pass.
- 2026-07-20: Quick fix, same branch — session list wasn't staying sorted by upcoming date. The API already sorts by `date` ascending on fetch (`src/lib/sessions.ts`/`sessions/route.ts`), but `SessionsView.tsx`'s `handleFormSubmit` appended newly-proposed sessions to the end of local state (`[...prev, saved]`) regardless of date, so proposing a near-term session while a later one was already on screen left the list out of order until a page refresh. Fixed at the single render point instead of patching every mutation callback: the list now renders `[...sessions].sort(...)` (by `date`, then `fromTime` as a same-day tiebreaker) rather than mapping `sessions` directly, so initial load, propose, edit, and alternate-time-propose all stay correctly ordered regardless of how the array was mutated. User explicitly deferred two related ideas raised in the same discussion — defaulting the propose form's date to today (concluded not worth it, people plan ahead and the date picker isn't the annoying one) and venue-defined time-slot durations with auto-filled end times (deferred until there's more than one venue on the roster; a lighter quick-duration-chip alternative was floated but also held off) — neither implemented. `npm run lint`, `npx vitest run` (140/140), and `npm run build` all pass; verified by reasoning through the comparator (ISO date strings sort correctly lexicographically) rather than a Playwright pass, per updated guidance on scoping browser verification to cases static checks can't cover.
- 2026-07-20: Real bug fix, same branch — user reported the grid's highlighted session-day column (and RSVP checkmark day) didn't match the day they'd proposed a session for (e.g. a session they read as "Wednesday" highlighted Thursday on the grid instead). Root cause was NOT in the grid logic (which is UTC-safe throughout — `groupSessionsByDate`/`hasSessionOnDate`/`isSessionConfirmedOnDate`/`isPlayerRsvpdIn` all compare on `session.date.slice(0, 10)` string slices, and `src/lib/teams.ts`'s `teamWindows`/`thisWeek` dates are built entirely with `getUTC*`/`setUTC*` methods) — it was `SessionsView.tsx`'s session-card date display, `new Date(session.date).toLocaleDateString("en-US", {...})`, called without a `timeZone` option, so it silently formatted using the browser/server's LOCAL timezone. For any negative-UTC-offset locale (all of the Americas), formatting a UTC-midnight timestamp locally rolls it back to the previous calendar day — confirmed via a targeted `TZ=America/Chicago node -e` repro (not Playwright): a session truly stored/correctly-grid-highlighted as Thu Jul 23 displayed on the card as "Wed, Jul 22" before the fix. The grid was right all along; the card text was lying to the user about which day they were looking at. Fixed by adding `timeZone: "UTC"` to the `toLocaleDateString` options — confirmed the same repro script; card and grid now agree. Traced with a single Neon SQL query against the dev branch's `Session` table (`information_schema.columns` confirmed `timestamp without time zone`, ruling out a Postgres storage-layer shift) plus the Node repro, rather than a Playwright walkthrough — this was the first real test of the "scope Playwright verification" feedback from earlier in the session, and a static/targeted check found and confirmed the root cause faster than a browser pass would have. `npm run lint`, `npx vitest run` (140/140), and `npm run build` all pass. Two sessions the user created while testing this (a cancelled Tue Jul 21 and a confirmed Thu Jul 23, both on `demo-team`) were left in place, not cleaned up — real test artifacts from the user's own exploration, not mine to touch unprompted.
- 2026-07-20: Two more quick fixes, same branch. **(1)** Session card times displayed as raw 24-hour `HH:MM` (e.g. "19:07–21:09"); switched to `formatTime(session.fromTime)}–{formatTime(session.toTime)` for AM/PM display (e.g. "7:07pm–9:09pm"). A correct, already-tested `formatTime` helper existed but was dead code — defined in `src/lib/teams.ts`, never imported anywhere. Importing it directly broke the client build: `teams.ts` also imports `@/lib/prisma`, and since `SessionsView.tsx` is a client component, pulling in anything from that module drags the whole `@prisma/adapter-pg`/`pg` graph (and its Node-only `fs`/`dns` requires) into the browser bundle — `next build` failed with `Module not found: Can't resolve 'dns'/'fs'`. Fixed by extracting `formatTime` into a new dependency-free `src/lib/format-time.ts` (and its existing 4 unit tests into `format-time.test.ts`), removing it from `teams.ts` entirely rather than leaving a client-unsafe duplicate. **(2)** INSZN's venue address was seeded as "Chicago, IL" — factually wrong, the venue is in Charlotte, NC. Root cause: a hardcoded placeholder in `prisma/seed.ts` from when the venue was first added, never corrected; since the seed's venue `upsert` uses `update: {}` (fields only set on first create), simply fixing the seed file wouldn't have corrected the already-seeded live row. Fixed both the seed file and the live dev-branch `Venue` row directly via a Neon SQL `UPDATE`. `npm run lint`, `npx vitest run` (140/140, same count — tests relocated not lost), and `npm run build` all pass (build failure from (1) was caught and fixed before this entry, not shipped).
- 2026-07-21: Open Venue Submission & Venues Page implemented on `feature/open-venue-submission`, planned via `/plan` per CLAUDE.md's `src/app/`/schema-file rules (touched `prisma/schema.prisma`, several `src/app/venues/**` routes, `src/lib/venues.ts`, `src/lib/sessions.ts`, `src/types/api.ts`, `SessionsView.tsx`). Removes the "admin-added only" venue bottleneck discussed at length with the user: venue creation already had no server-side gate (the Server Action was just unlinked), so this is mostly a discoverability/routing change, not a new trust boundary. Added `Venue.openTime`/`closeTime` (optional `"HH:MM"`, informational only, migration `20260721042742_add_venue_hours`). Replaced `src/app/admin/venues/` with `src/app/venues/{page.tsx, new/page.tsx, [venueId]/edit/page.tsx, [venueId]/delete/page.tsx}` + a consolidated `actions.ts` (`createVenue`/`updateVenue`/`deleteVenue`, all Server Actions, no ownership check — same trust model as every other write). `deleteVenue` checks `session.count({ where: { venueId } })` and throws if any exist before deleting — this turned out to be load-bearing, not just defensive: Prisma's default `onDelete` for `Session.venueId` (optional, unspecified) is `SetNull`, so an unguarded delete would have silently orphaned session history instead of erroring (confirmed live via Playwright: deleting INSZN, which has sessions, correctly throws "Cannot delete this venue — 2 session(s) still reference it." and leaves the row untouched per a follow-up SQL check; deleting an unused test venue succeeds). Added `getVenueById` to `src/lib/venues.ts` for the edit/delete pages. Relocated the `VENUE_TYPE_LABELS` constant (previously duplicated verbatim in the admin page and `SessionsView.tsx`) into `src/types/api.ts` as `Record<VenueType, string>`. Added a "Venues" link to `SessionsView.tsx`'s header, to the right of "+ Propose" per the user's explicit ordering — no changes needed to the venue-select dropdown itself, which turned out to already show `{name} ({type label})` rather than a bare name, satisfying that part of the original ask without any code change. 15 new/extended unit tests (`venues.test.ts` extended for the new fields + `getVenueById`; new `src/app/venues/actions.test.ts`, first test file in the repo needing a `next/navigation` mock for `redirect`); `npm run lint`, `npx vitest run` (152/152), and `npm run build` all pass. Hit one unrelated snag verifying manually: `prisma migrate dev` didn't auto-regenerate the client this time, so `npm run build` initially failed on `prisma/seed.ts`'s new `openTime`/`closeTime` fields — fixed with an explicit `npx prisma generate`. Also hit the same recurring stale-dev-server issue noted elsewhere in this file's history (a `next-server` process on port 3000 left over from a previous session, predating this branch's schema/route changes) — killed it and started fresh before the Playwright pass. `prisma/seed.ts`'s demo-team-reset logic (`team.deleteMany` then recreate) is separately broken by accumulated test-session data violating a `RESTRICT` FK constraint — pre-existing, unrelated to this feature, not fixed here; backfilled INSZN's seeded hours (6am–9pm) directly via Neon SQL instead of running the full seed script. Full Playwright walkthrough against `demo-team`: add → edit → delete-blocked (has sessions) → delete-succeeds (unused venue) → "Venues" link renders right of "+ Propose" and navigates correctly — all confirmed. Docs (`AGENTS.md`, `practiceRuns-ProjectOverview.md`, `practiceRuns-Architecture.md` incl. the system-context mermaid diagram, `practiceRuns-ProjectPlan.html`) updated to drop the "admin-added only"/`/admin/venues` framing throughout. Status set to Completed — ready for commit per ai-interaction.md workflow.
- 2026-07-21: Same branch — user feedback after reviewing the shipped venues pages: too narrow, didn't match the grid page's aesthetic, and no way back to the team page. Root cause: the four `/venues` pages were carried over from the original `/admin/venues` styling, which predates the app's real design system and used bare default Tailwind utilities (`bg-gray-950`, `bg-orange-600`, `bg-gray-800`, etc.) instead of the themed tokens (`bg-bg`/`bg-surface`/`bg-accent`/`text-gold`/`bg-danger`, defined in `globals.css`'s `@theme` block) that `TeamGrid.tsx`/`EditDrawer.tsx`/`NamePicker.tsx` actually use. Planned via `/plan` per CLAUDE.md's `src/app/` rule. Restyled all four pages to the real token system and `TeamGrid`'s exact outer-shell width (`min-h-screen bg-bg text-text px-3 py-4 flex justify-center` + `w-full max-w-md lg:max-w-xl`, previously a much narrower plain `max-w-lg`), reused `TeamGrid`'s header-card chrome (`rounded-xl bg-surface border border-border`) and `EditDrawer`'s input/button conventions; venue-type badges switched to the `bg-gold-soft`/`text-gold` treatment already used for session grid indicators, giving the list some visual interest instead of a flat gray pill. Added a new `src/components/BackButton.tsx` (`"use client"`, calls `router.back()`) at the top of all four pages — there was no back-navigation pattern anywhere in the app before this, confirmed by search. Deliberately used browser-history `router.back()` rather than a hardcoded `/team/[slug]` link or a `?from=` query param threaded through every route and Server Action, since it correctly returns to wherever the user actually came from with far less plumbing, and a small client component for real interactivity is normal use of `'use client'` per this project's own convention — verified live via Playwright: Team page → "Venues" → "‹ Back" lands back on `/team/demo-team` exactly. Also caught and cleaned up a leftover `TEST` venue from the prior session's manual verification pass. No logic/schema changes — styling and one new nav component only. `npm run lint`, `npx vitest run` (152/152), and `npm run build` all pass; full Playwright screenshot review of all four pages plus the back-navigation chain confirmed the redesign matches the grid page's width/aesthetic.
- 2026-07-21: Same branch, two more follow-ups from the user testing the venues feature. **(1) Cost model**: `Venue.costPerSession` renamed to `costPerHour` — the user compared against INSZN's real booking site and found it prices linearly per hour ($50/hr: 1hr=$50, 2hr=$100, 3hr=$150), so a flat "typical session cost" silently assumed one duration. `Session.costTotal` (the number that actually drives cost-split math) is untouched — this field has only ever been a display/reference value. Migration `20260721060000_rename_venue_cost_per_hour` used a hand-written `ALTER TABLE ... RENAME COLUMN` (not the drop+add Prisma's non-interactive `migrate dev` wanted to generate) specifically to preserve the existing row's data through the schema change, since `migrate dev` refuses to run non-interactively once it detects data loss and neither `--create-only` nor any flag bypasses that — resolved by hand-authoring the migration file and applying it with `migrate deploy` (which doesn't have that interactive guard) instead. Backfilled INSZN's actual value (5000 = $50.00/hr, was still 10000 from the old "$100 typical" era) via Neon SQL afterward. This rename also exposed a real, not-just-cosmetic bug in `SessionsView.tsx`'s propose-session form: its "Total cost" placeholder used to just echo the venue's flat reference number regardless of chosen duration — under an hourly rate that would've shown "$50" as the suggested cost even for a 3-hour booking. Fixed by adding `estimateCostFromHourlyRate()`, which parses the form's `fromTime`/`toTime` into a duration and multiplies by the venue's rate, falling back to the bare rate when duration isn't computable yet; confirmed live that an INSZN 18:00–21:00 (3hr) selection now suggests "150.00", not "50". **(2) Deterministic back navigation**: replaced last entry's `router.back()`-based `BackButton` (deleted) after the user found a real problem with it — after Team → Venues → +Add Venue → submit (redirected back to Venues), clicking Back walked backward through that same browser-history chain (→ new-venue form → original Venues visit → team) instead of going straight to the grid. Replaced with static links carrying the team slug through a `from` query param: `SessionsView`'s "Venues" link is now `/venues?from=<slug>`; the list page shows "‹ Back to Grid" → `/team/<from>` only when `from` is present, and forwards `fromQuery` on its "+ Add Venue"/Edit/Delete links; `new`/`edit`/`delete` pages all show a static "‹ Back to Venues" → `/venues?from=<from>` (never wherever browser history happened to be) and carry `from` through their forms via a hidden input so the Server Actions' post-submit redirect (`venuesRedirectTarget()` helper in `actions.ts`) preserves it. Verified the user's exact reported chain end-to-end via Playwright: Team → Venues → +Add Venue → submit → Back lands on `/venues` (not the new-venue form) → Back again lands on `/team/demo-team`; also confirmed Edit → Back-without-saving correctly lands on `/venues`. 5 new/changed test cases in `venues/actions.test.ts` (redirect-with-from vs. bare-redirect for all three actions) plus the costPerHour rename across existing fixtures; `npm run lint`, `npx vitest run` (156/156), and `npm run build` all pass (note: `/venues` and `/venues/new` are now dynamically rendered instead of static, since they read `searchParams` — expected). Docs (`practiceRuns-ProjectOverview.md`, `practiceRuns-Architecture.md`, `practiceRuns-ProjectPlan.html`) updated for the field rename. Left two pre-existing test venues ("test"/"test2") and the seed script's separately-broken demo-team-reset logic untouched — not this session's scope.
- 2026-07-21: Same branch — follow-up on the cost-per-hour work above: the user pointed out the estimated cost was only ever a placeholder (ghost text), not a real submitted value, so a proposer who left it untouched would submit a session with no `costTotal` at all despite seeing "$100" in the field. Fixed by having `SessionsView.tsx` actually fill `costTotal` (not just hint it) whenever the venue, from-time, or to-time changes: a new `costManuallyEdited` flag, set once the proposer types into the cost field themselves, gates a `maybeAutoFillCost()` call added to the venue-select/from-time/to-time `onChange` handlers so auto-fill never clobbers a deliberate override; `startEdit` sets the flag `true` immediately (an existing session's agreed cost shouldn't silently recompute if its duration is later edited) while `startAlternate`/`resetForm` reset it to `false` (fresh proposals should auto-fill). The placeholder logic from the prior entry is unchanged and still covers the case before both times are picked. Verified live: selecting INSZN 18:00–20:00 fills the field with an actual `value="100.00"` (confirmed via DOM inspection, not just the accessible-name/placeholder), and manually overriding to $80 then extending the end time to 21:00 correctly leaves the $80 in place rather than recomputing to $150. `npm run lint`, `npx vitest run` (156/156), and `npm run build` all pass — no test file exists for `SessionsView.tsx` (it's verified via Playwright per existing project convention), so no new automated tests were added for this pure-UI behavior.
- 2026-07-21: Feature documented and planned via `/plan` per CLAUDE.md's `src/app/` rule. Scope confirmed with user: shareable link only (goal 1 of the original 3-layer notifications brainstorm); Web Push and SMS/Twilio explicitly held for later. Confirmed: identity-free viewing / identity-gated RSVP, persistent per-card "Share" button (not auto-copy-on-propose, matching this app's no-toast convention), Web Share API with clipboard fallback, and to follow the existing direct-Prisma-in-Server-Components pattern over the conflicting `.claude/rules/app-router.md` rule for the new page. Status set to In Progress. Mid-implementation, user added that they'd rather use AWS (SNS/Pinpoint) than Twilio for the eventual SMS layer, to build cloud skills for work — noted for when that evaluation actually happens, doesn't change this feature's scope.
- 2026-07-21: Implemented on `feature/session-shareable-link`. Added `getSessionForTeam(teamId, sessionId)` to `src/lib/sessions.ts` (team-scoped `findFirst`, mirrors `getVenueById`'s shape) — no schema change, `Session`/`Rsvp` already existed from Phase 3. Extracted the read-only parts of a session card (venue/date/time header, confirmed badge, cancelled banner, cost-split block, RSVP list) out of `SessionsView.tsx`'s per-session JSX into a new presentational `src/components/SessionSummary.tsx`, shared between the existing inline list and the new detail page — proposer-only actions (Edit/Confirm/Cancel/Delete) deliberately stayed out of it and out of the new page entirely, since the user's spec only asked for view + RSVP there. Extracted the optimistic-PUT RSVP logic into a dependency-free `src/lib/session-rsvp.ts` (`putRsvp` + `RsvpError`) reused by both consumers, each still owning its own optimistic-update/rollback state shape (array vs. single object). Added `src/components/ShareButton.tsx` — Web Share API when available (ignoring a cancelled share's `AbortError` rather than falling back), else `navigator.clipboard.writeText` with a transient "Copied!" text swap (no toast) — and wired it into every session card via a `path` prop so the absolute URL is only built inside the click handler (`window.location.origin` isn't available at render time). New route `src/app/team/[slug]/sessions/[sessionId]/page.tsx` (Server Component, direct `getTeamGrid`/`getSessionForTeam` calls, `notFound()` on either miss) renders a new `src/components/SessionDetailView.tsx` client component: session info always visible via `SessionSummary`, RSVP In/Out buttons if `useIdentity` resolves a valid stored player for the team, otherwise a compact tap-to-select name row (not `NamePicker`'s full-page gate) — same stale-identity auto-clear effect as `TeamGrid`. 7 new unit tests (`getSessionForTeam` found/not-found/wrong-team; `putRsvp`'s request shape, success, server-error, and unparseable-error-body cases) — 163/163 total. `npm run lint` and `npm run build` both pass. Verified end-to-end via Playwright against `demo-team` after killing a stale pre-existing dev server (same recurring gotcha noted elsewhere in this file): Share correctly calls `navigator.share` with the right URL/title when available, correctly falls back to clipboard + "Copied!" when it isn't, and correctly does *not* fall back when the user cancels the native share sheet (`AbortError`); opening a session link with `localStorage` cleared shows full session details with zero identity gate; RSVPing prompts the inline name picker, and RSVPing "in" as a fresh identity updates the cost-split/RSVP-list live and is reflected back on `/team/[slug]`'s inline card; a bogus session ID 404s. Test RSVP was reverted afterward to match the pre-test baseline. Docs updated: `practiceRuns-ProjectOverview.md` (new route in Routes & API table, two new Decisions log rows) and `AGENTS.md` (Current Status). Status set to Completed — ready for commit per ai-interaction.md workflow.
- 2026-07-21: Feature discussed conceptually in group-chat-gap terms, planned via `/plan` per CLAUDE.md's schema-file rule. Clarifying questions asked and answered: manual propose-one-at-a-time (not bulk/season import) for this pass; "Game Day" section placed above "Sessions" (not merged into one list, not below); section labeled "Game Day"; below-threshold games get a distinct urgent/red forfeit warning rather than reusing the neutral amber "need more to book" wording. Plan approved (`/Users/tmdbah/.claude/plans/i-have-somewhat-of-lazy-wren.md`). Status set to In Progress.
- 2026-07-21: Implemented on `feature/game-day`. Added `SessionKind` enum (`PRACTICE` default, `GAME`) + `Session.kind` field via migration `20260721235121_add_session_kind` (plain additive column with a DB default, same shape as the earlier `add_session_status` migration — no backfill needed, every existing row correctly defaults to `PRACTICE`). Added `kind`/`SESSION_KIND_LABELS` to `types/api.ts` (`SessionResponse` required, `CreateSessionBody` optional; deliberately excluded from `EditSessionBody` so `kind` is immutable via edit); one line in `src/lib/sessions.ts`'s `toSessionResponse`; POST `/api/teams/[slug]/sessions` validates `kind` against the generated enum (mirroring the existing `VenueType` idiom in `venues/actions.ts`) and passes it through as `kind ?? undefined` so an omitted value lets the DB default apply. Decoupled `SessionSummary.tsx`'s cost/headcount box into two independently-gated pieces (`showHeadcount = minPlayers != null`, `showCost = isRented && costTotal != null`, previously a single combined condition) and added an exported `headcountStatus(kind, minPlayers, inCount)` pure helper driving copy/color: PRACTICE keeps the existing neutral amber "Need N more"/green "✓ Enough to book"; GAME gets red/urgent "Need N more to avoid forfeit"/green "✓ Have enough to play". Extracted the per-session list-item JSX (previously duplicated-in-place inside `SessionsView.tsx`) into a new `src/components/SessionsSection.tsx`, parameterized by `kind`/`title`/`proposeLabel`/the filtered+sorted session array/all existing kind-agnostic handlers and transient UI state passed through as props — "Mark as Booked" only renders for `kind === "PRACTICE"`. `SessionsView.tsx` is now the orchestrator: new `formKind` state, a `handleProposeClick(kind)` that opens the one shared propose/edit form pre-set to whichever section's button was clicked (defaulting `minPlayers` to `"5"` only for a fresh GAME proposal), `showCostField = isRentedGym && formKind !== "GAME"` and `showMinPlayersField = formKind === "GAME" || isRentedGym` replacing the old single `isRentedGym` gate on both fields, and two `<SessionsSection>` renders ("Game Day" above "Sessions", the pre-existing "Venues" link now living on the Sessions section's header). Added an optional second demo venue (Northside Community Center, `OPEN_GYM`) and one demo `GAME` session (next Thursday, 7:15–9:00pm, `minPlayers: 5`) to `prisma/seed.ts` — the seed script's `team.deleteMany`-based reset was NOT re-run against the live dev branch, since prior history in this file already documents that it hits a pre-existing `RESTRICT` FK violation once real sessions exist on `demo-team` (unrelated to this feature, not fixed here); the new UI was instead verified by proposing a real Game through the running app and deleting it afterward. 9 new/changed unit tests (`sessions.test.ts` GAME-kind mapping; 3 new POST-route cases for `kind` accepted/omitted/invalid; 1 PATCH-route guard asserting `kind` is never in the update payload; new `SessionSummary.test.ts` covering all 5 `headcountStatus` branches) — 172/172 total. `npm run lint`, `npx vitest run`, and `npm run build` all pass. Verified end-to-end via Playwright against `demo-team`: "Game Day" renders above "Sessions" with a correct empty state; proposing a Game shows no cost field even with the RENTED_GYM-typed INSZN venue selected, prefills "Min to avoid forfeit" to 5, and the created card shows the red "Need 5 more to avoid forfeit" text (confirmed via computed `className`, since screenshot capture times out in this environment per prior history) with no "Mark as Booked" button; RSVPing In updated the count live (1/5, "Need 4 more to avoid forfeit", ✅ Caden) without disturbing the pre-existing practice sessions' unrelated amber "Need 10 more" cards; delete-confirm correctly read "Delete this game?"/`aria-label="Delete game"`. Test game deleted afterward, demo team confirmed back to its pre-test baseline; zero console errors throughout. Docs updated: `AGENTS.md` (Also-shipped bullet), `practiceRuns-ProjectOverview.md` (new `SessionKind` enum + `Session.kind` in the reproduced schema block, a new Features bullet under V3, the POST route description, and three new Decisions Log rows), `practiceRuns-Architecture.md` (`Session` entity diagram + two new §7 decision rows). Status set to Completed — ready for commit per ai-interaction.md workflow.
- 2026-07-21: Feature documented and planned via `/plan` per CLAUDE.md's >3-files rule, following a codebase audit confirming no Phase 4 work existed yet and reconciling one real doc gap (the `Venue.openTime`/`closeTime` fields were missing from the reproduced schema blocks in `practiceRuns-ProjectOverview.md`/`practiceRuns-Architecture.md`/`practiceRuns-ProjectPlan.html`, fixed before planning began). User confirmed scripted first-session state (guide-to-tap) over a coach-marks library, with reasoning: the player's own row starts all-`UNAVAILABLE`, so having them actually perform the tap/save doubles as their first real data entry. Plan approved (`/Users/tmdbah/.claude/plans/recursive-riding-river.md`). Status set to In Progress.
- 2026-07-21: Implemented on `feature/onboarding-tour`. Added `src/lib/tour-steps.ts` (`TourStep`/`TourEvent` types, `advanceTour` reducer, `TOUR_COPY`), `src/lib/tour-position.ts` (`computeTooltipPlacement`, pure viewport-edge-aware placement math), `src/hooks/use-tour.ts` (`useTourSeen`, a structural clone of `use-identity.ts` including its dual-`useSyncExternalStore` hydration-safe pattern; storage key `practice-runs:tour:${slug}`, value the literal string `"seen"`), and `src/components/OnboardingTour.tsx` (presentational spotlight-ring + tooltip overlay, no knowledge of grids/players/storage). `TeamGrid.tsx` gained `tourStep` state and a `handleNameSelected` wrapper around `NamePicker`'s `onSelect` (arms the tour only when `!hasSeenTour`, since `NamePicker` only ever renders for a brand-new device) plus `handleTourAdvance` (writes `hasSeenTour` only when `advanceTour` returns `"complete"`, i.e. on skip or the final "Got it"). `AvailabilityGrid.tsx` gained two optional props (`tourStep`, `onTourAdvance`, both inert by default), three refs attached to already-existing elements (the current player's Monday cell, the mode-toggle wrapper, a new bare wrapper around the Team Window block — `GridCell.tsx`/`TeamWindowCard.tsx`/`NamePicker.tsx`/`EditDrawer.tsx` needed zero changes), a `useLayoutEffect`+resize-listener pair measuring whichever element the current step targets, and two event-firing call sites (`handleSave` fires `cell-saved` at the optimistic-save moment; the toggle button's `onClick` fires `toggle-tapped`). One deviation from the approved plan, caught by `npm run lint`: forcing the grid back to this-week mode for the Team Window step was originally planned as a `useLayoutEffect` calling `setMode`, but ESLint's `react-hooks/set-state-in-effect` rule rejected that (setState-in-effect, not a real "derive state from a changed input" case) — fixed by folding the force directly into the toggle button's `onClick` instead (tapping either button during the tour's toggle step always lands on this-week mode, since the next step needs it), which is a real user-triggered event handler, not a bare effect. 18 new unit tests (`tour-steps.test.ts` 8 cases, `tour-position.test.ts` 5 cases, `use-tour.test.ts` 6 cases, mirroring `use-identity.test.ts`'s fixture/isolation conventions exactly) — 191/191 total. `npm run lint`, `npx vitest run`, and `npm run build` all pass; zero schema/migration/API changes as planned. Verified end-to-end via Playwright against `demo-team` (after killing a stale pre-existing dev server, the same recurring gotcha noted elsewhere in this file's history): cleared storage → picked Amir → tour armed on the real Monday cell → tapped it → real `EditDrawer` opened with the tour overlay correctly hidden → saved Anytime → tour reappeared on the toggle step → tapped "Usual" (deliberately, to test the forced-mode logic) → confirmed via the cell's aria-label that the grid was still in this-week mode, not usual, proving the force-to-this-week toggle logic works → "Team Window" step → "Got it" → `localStorage["practice-runs:tour:demo-team"] === "seen"`, zero console errors throughout. Also confirmed: a returning player with valid stored identity never sees the tour (straight to the grid); a second fresh identity (Caden) got the tour armed and "Skip tour" hid it immediately; at iPhone SE (375×667) the tooltip never clipped the viewport and the tour overlay + `EditDrawer` were confirmed to never coexist (tour mounts only when `tourStep && !activeEdit`). Reverted the one real data change made during testing (Amir's Monday This Week override) via the UI's own "Reset to Usual" button, confirmed back to the pre-test `UNAVAILABLE (inherited)` baseline. Docs updated: `AGENTS.md` (top status line + Current Status section), `practiceRuns-ProjectOverview.md` (status line, V4 features bullet, UI/UX Key Screens table row, two Decisions log rows — one factual update, one new row on the scripted-vs-coach-marks choice — Build Phases Phase 4 entry, AI Working Rules current-phase line, and the resolved Open Questions bullet), `practiceRuns-Architecture.md` (status line, two new §7 decision rows, Phase 4 Gantt entry split into a `done` onboarding-tour bar plus the still-planned demo-team/cron bar), `practiceRuns-ProjectPlan.html` (sidebar status, Decisions log Onboarding card rewritten as shipped, UX flow node badge swapped from amber "Phase 4" to teal "Shipped", Build Phases Phase 4 card). Status set to Completed — ready for commit per ai-interaction.md workflow.
- 2026-07-22: User feedback after trying v1 live: the sequence taught mechanics out of causal order (created a This Week override first without ever explaining Usual Schedule, This Week, or the inherited/overridden visual language) and never mentioned Sessions/Game Day at all, so a new player wouldn't know those exist. Discussed the app's full feature inventory and tiered what's tour-worthy; user confirmed via follow-up questions: no dedicated welcome step (fold framing into step 1's copy), Sessions/Game Day get exactly one combined non-interactive info step, the This Week step is explain-only (no second edit), Venues stays out of the tour entirely. Replanned via `/plan` (plan mode auto-exited once mid-session before the plan was complete — re-entered and finished it properly before implementing). Plan approved.
- 2026-07-22: v2 resequence implemented on the same branch. `src/lib/tour-steps.ts` rewritten: `TourStep` is now `"usual" | "toggle" | "overrides" | "team-window" | "sessions" | "complete"` (renamed from `"cell"`), `TOUR_COPY` gained a `primaryLabel: string | null` field (null = interactive step, no button besides Skip; non-null = shown as the primary button — "Next" for `overrides`/`team-window`, "Got it" for the new final step `sessions`), and added `TOUR_STEP_ORDER` for a step-counter. `OnboardingTour.tsx` generalized: replaced the old hardcoded `isFinalStep` check with `copy.primaryLabel`-driven rendering (Skip always shown; primary button shown whenever `primaryLabel` is set) and added a "Step X of 5" line. `AvailabilityGrid.tsx`: `mode`'s `useState` now takes a lazy initializer (`tourStep === "usual" ? "usual" : "this-week"`) instead of a hardcoded default — correct because `TeamGrid.handleNameSelected` sets `tourStep` to `"usual"` in the same handler as `setPlayerId`, so the prop is already correct on this component's first-ever mount; the measurement ternary now maps both `"usual"` and `"overrides"` to the same existing `cellRef` (no new ref — both steps spotlight the identical Monday cell, and the `overrides` step's inherited/faded display falls out for free from the existing `getWeekCell` Usual-inherits-into-This-Week merge, no new logic needed); added an `onDrawerOpenChange` callback prop, called once each in `openDrawer`/`closeDrawer`. `TeamGrid.tsx`: since the Game Day/Sessions section lives outside `AvailabilityGrid.tsx`, `TeamGrid` gained its own `sessionsRef` (attached directly to its pre-existing `<div className="px-1 pb-8 mt-6">` wrapper around `<SessionsView>`, covering both sections in one spotlight with zero changes to `SessionsView.tsx`/`SessionsSection.tsx`), its own tiny measurement effect scoped to `tourStep === "sessions"`, an `isDrawerOpen` state fed by the new `onDrawerOpenChange` callback, and a second, independent `<OnboardingTour>` mount gated on `tourStep === "sessions" && !isDrawerOpen` — mirroring `AvailabilityGrid`'s own `!activeEdit` guard for its four steps, rather than relocating all tour ownership up to `TeamGrid` (a considered, rejected, much larger alternative). `tour-steps.test.ts` fully rewritten for the 5-step reducer (11 cases including an end-to-end sequence-order check against `TOUR_STEP_ORDER`) — 196/196 total tests pass; `npm run lint` and `npm run build` also pass. Verified end-to-end via Playwright against `demo-team`: grid now opens in **Usual** mode (not This Week) for step 1; tapped Caden's Monday cell, saved Anytime via the real `/default` PATCH; tapped "This Week", confirmed via computed `className`/`aria-label` that the same Monday cell now renders faded (`bg-accent/30`) with the `(inherited)` label — the intended "here's the same day, now inherited" moment, working with zero new merge logic; stepped through `overrides` ("Next") → `team-window` (rewritten gold-highlight/swipe copy, "Next") → `sessions` (spotlight correctly covered both "Game Day" and "Sessions" headings in one rect) → "Got it" → `hasSeenTour` persisted. Also specifically verified the new cross-component drawer guard: tapped an unrelated cell (Darius/Tuesday) while the `sessions` tooltip was showing — confirmed only the real `EditDrawer` was present (tour hidden), and closing it without saving made the tour tooltip reappear. Re-confirmed iPhone SE (375×667, now with the added step-counter line) has no clipping. One real bug caught mid-verification, unrelated to this session's changes: a stray `DateOverride` row (Amir, This Week Monday, `ANYTIME`) survived from an earlier verification pass in this same conversation and was masking the `overrides` step's inherited-display check — traced via direct Neon SQL against the dev branch (`DateOverride`/`DayDefault` tables), confirmed `AvailabilityGrid`'s `handleSave` "usual" branch only ever calls the `/default` endpoint (never `/override`, ruling out a code regression), deleted the stray row, and re-ran the check cleanly on a fresh player (Caden) to get an uncontaminated result. All test data (`DayDefault` rows for Amir/Caden/Darius) confirmed back to the `UNAVAILABLE` baseline afterward via SQL; no stray `DateOverride` rows remain on `demo-team`. Docs updated: `current-feature.md` Goals/Notes rewritten for the 5-step sequence and the `TeamGrid`-owns-`sessions`-step architecture; `AGENTS.md`/`practiceRuns-ProjectOverview.md`/`practiceRuns-Architecture.md`/`practiceRuns-ProjectPlan.html` updated wherever they described the old 3-step sequence. Status remains Completed — ready for commit per ai-interaction.md workflow.
- 2026-07-22: Bug report — the `sessions` step wasn't rendering consistently in VS Code's built-in Simple Browser (worked fine in a real browser), going off-screen depending on the panel's size. Root cause: that step spotlighted the *entire* Game Day + Sessions wrapper (`TeamGrid`'s `sessionsRef`), which can be far taller than the viewport (~900px measured earlier in this same conversation) — `computeTooltipPlacement` assumes a target that fits on screen, and a target taller than the viewport breaks both its "below" and "above" branches; VS Code's narrower/shorter embedded panel just exposed the mismatch more easily than a full browser window. Asked the user two scoping questions and got answers: spotlight narrows to the Game Day header + its first game/session card (not the full two-section wrapper, and not just the bare header) and the page should auto-scroll that into view when the step starts. Implemented: `SessionsSection.tsx` gained two optional ref props (`headerRef` on the header row div, `firstRowRef` attached to whichever renders first — the empty-state `<p>` or the first `<li>`); `SessionsView.tsx` threads two new optional props (`gameHeaderRef`, `gameFirstRowRef`) through to only the `"GAME"` `<SessionsSection>` call (the `"PRACTICE"`/Sessions one is untouched); `TeamGrid.tsx` replaced its old single `sessionsRef`-on-the-whole-wrapper with these two refs, computes a union `DOMRect` covering both (via the `DOMRect` constructor, so it satisfies `OnboardingTour`'s existing prop type) with a fallback to the header rect alone when there's no first row yet, added a `scroll` listener alongside the existing `resize` listener (scrolling changes `getBoundingClientRect()` results just as resizing does — the original code only listened for resize), and added a separate `useEffect` that calls `gameHeaderRef.current?.scrollIntoView({ behavior: "smooth", block: "start" })` exactly once when `tourStep` newly becomes `"sessions"`. Verified via Playwright at 420×500 the new spotlight is 216px tall (down from ~900px) with zero clipping; at an even more extreme 420×350 found a second, related bug: when the target + tooltip combined height still exceeds a very short viewport, neither the "below" nor "above" placement branch in `computeTooltipPlacement` fits, and the existing horizontal clamp had no vertical counterpart — the tooltip rendered mostly above the viewport top. Fixed by adding a vertical clamp (`Math.max(GAP, Math.min(idealTop, viewport.height - tooltip.height - GAP))`) mirroring the pre-existing horizontal one; confirmed live afterward (`top: 8`, no clipping) at the same 420×350 viewport that previously failed. 2 new unit tests in `tour-position.test.ts` (the tall-target/short-viewport clamp case) — 197/197 total. `npm run lint`, `npx vitest run`, and `npm run build` all pass. Test data (`DayDefault` rows touched for Elijah/Isaiah/Amir during verification) confirmed back to `UNAVAILABLE` via direct SQL afterward; zero `DateOverride` rows remain on `demo-team`. Status remains Completed — ready for commit.
